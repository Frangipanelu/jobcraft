# PRODUCT.md — JobCraft 求职助手 · 产品需求边界

> 本文档定义 JobCraft 的产品愿景、MVP 功能边界、Non-Goals 与核心验收标准。所有功能开发必须以此为边界，防止过度设计。

> **2026-07-30 更新**：架构从四步线性流程（经历→岗位→面试准备→复盘）重构为求职 CRM 模式。
> JD 分析改为原子功能，投递记录（resume_submission）作为 pipeline 核心，面试准备/复盘绑定到投递。

## 1. 产品愿景

JobCraft（求职助手）帮助求职者系统性地管理求职全流程：

> **🏠 求职路线（主页）→ 📋 经历卡 → 🔍 JD 分析库 → 🎤 面试准备 → 📝 面试复盘**

以投递记录为主线，AI 辅助每个环节，用户自主决策何时进入下一阶段。

## 2. MVP 核心功能列表

### 2.0 求职路线（Dashboard）— 主页

- **只显示有简历的投递**（投递记录 = 简历已生成，不存在空投递）。
- **按时间倒序展示**，每条记录包含岗位、公司、状态。
- **5 步按钮矩阵**：JD 分析 / 润色卡片 / 简历 / 🎤面试准备 / 📝复盘，按投递状态点亮/灰掉。
- **状态流转**：用户通过下拉框切换（已投递 → 面试邀约 → 一面 → 二面 → Offer / 已关闭）。
- **入口作用**：从主页进入所有功能页，面试准备/复盘不占导航位。
- **空状态**：引导用户去 JD 分析库开始第一次分析。

### 2.1 经历梳理（Experience Cards）

- 支持用户上传简历/输入文本，存为 `raw_text`（原始文本，唯一必填）。
- 每张卡包含：标题（用户取名）、原始文本（用户自由写）、标签（平铺关键词）。
- AI 结构化缓存按需生成/刷新，包含：一句话总结 + 1~3 条成就（S/A/R 结构，Action 内含困难与解决）。
- 标签由 LLM 推荐（3-5 个扁平关键词）+ 用户手动编辑确认，不打在成就上，只打在整段经历上。
- 标签作用是筛选 + LLM 上下文 hint，不锁死解读方向。
- 版本历史面板：展示哪些投递在使用哪些润色版本。
- 支持手动创建、编辑、删除经历卡。
- 不做多 Agent 架构，每个步骤是确定性单次 LLM 调用。

### 2.2 JD 分析库（独立工作台）

- **原子功能**：不绑定卡片、不绑定流程步骤，可被多个场景复用。
- 输入 JD 文本 → AI 提取 ATS 画像（岗位/经验/学历/技能/关键词/D1-D8）。
- 推荐匹配的经历卡片。
- 操作按钮：「为该 JD 定制简历」→ 触发完整流程：检查卡片 → gap 润色（弹窗预览）→ 确认 → saveResume → 自动创建投递记录（含 resume_markdown）→ 跳转主页。
- 若尚无经历卡片，跳转至经历卡页引导创建。
- **可不创建投递**：JD 分析可以独立完成并存着，供后续参考。

### 2.3 投递记录（Pipeline 核心）

- **创建方式**：仅通过 JD 分析页「为该 JD 定制简历」流程自动创建，不支持手动新建。
- 记录创建即包含 `resume_markdown`（从 card_versions 模板生成），不存在空投递。
- 支撑状态流转：已投递 → 面试邀约 → 一面 → 二面 → Offer / 已关闭。
- 快照所用润色版本（card_version_ids）。
- 关联面试准备稿和复盘记录。
- 支持上传实际投出的简历文件。

### 2.4 面试准备（从投递进入）

- 基于投递记录的 JD + 实际投出的简历 + 经历卡素材生成预测面试题。
- 轮次选择：技术面 / 业务面 / HR 面。
- 每道题提供答题要点（answer_points）与可关联的经历卡。
- 生成完整的面试准备稿（Markdown / HTML）。

### 2.5 面试复盘（从投递进入）

- 支持上传面试记录文本/音频转写文本。
- AI 自动识别面试官问题与面试者回答，按时间顺序配对 QA。
- 对每个核心问题进行评分、意图分析、标准答案、诊断反馈与改进建议。
- 输出整体评分、优势、劣势与行动项。
- 标注第几面（一面/二面/HR面）。

### 2.6 闭环聚合（v0.6 新增）

- **跨JD聚合**：汇总同类岗位（如"后端开发"）的多个 JD 分析，提炼共性能力要求与公司差异。
- **跨复盘聚合**：汇总同一公司的多次面试复盘，提炼高频考察维度、典型题型、难度分布。
- **反哺经历卡**：根据复盘发现的能力短板，自动建议用户补充相关经历卡。
- **反哺面试准备**：在公司画像引导下，面试准备更有针对性。

## 3. Non-Goals（第一版不做）

以下功能明确排除在 MVP 之外，防止范围蔓延：

- 用户注册 / 登录 / 权限体系（默认单用户 user_id=1，后续再接入）。
- 简历 PDF 自动排版生成（仅输出 Markdown/Word，不渲染精美模板）。
- 实时语音面试/AI 模拟面试官（仅支持文本记录复盘）。
- 多轮对话式面试辅导（单次输入、单次输出）。
- 商业化支付、订阅、团队协作。
- 复杂的数据持久化（MySQL 仅用于可选工具，核心状态使用文件/内存即可）。
- 自动联网抓取实时岗位信息（公司背调除外，使用 Tavily 已有能力）。

## 4. 核心用户场景与验收标准

### 4.1 场景一：上传简历生成经历卡

**Given**：用户粘贴一段项目经历文本。  
**When**：调用经历卡抽取接口。  
**Then**：

- 返回 1-5 张结构化经历卡。
- 每张卡包含完整的 STAR 字段。
- 维度标签覆盖 D1-D8 中的至少 3 个。
- 接口响应时间 < 3 秒（在 Groq API 正常时）。

### 4.2 场景二：输入 JD 生成匹配分析

**Given**：用户输入岗位 JD。  
**When**：调用岗位分析接口。  
**Then**：

- 提取岗位关键词、硬技能、软技能。
- 返回匹配分数（0-100）与匹配等级。
- 指出至少 3 条缺失项与 3 条优化建议。
- 接口响应时间 < 5 秒。

### 4.3 场景三：生成面试准备稿

**Given**：已完成岗位分析并拥有经历卡。  
**When**：调用面试准备接口。  
**Then**：

- 生成覆盖 8 个维度的预测题。
- 每道题提供 2-4 条答题要点。
- 输出完整的准备稿 Markdown 与 HTML。
- 接口响应时间 < 8 秒。

### 4.4 场景四：上传面试记录进行复盘

**Given**：用户上传面试对话记录。  
**When**：调用面试复盘接口。  
**Then**：

- 正确识别面试官与面试者身份，不反调。
- 按时间顺序配对 QA，不跳跃、不合并。
- 过滤闲聊/过渡/确认类发言。
- 每个核心问题给出 intent、expected_answer、feedback、suggestions、score。
- 接口响应时间 < 10 秒。

## 5. 性能与体验基线

- 普通 API 调用 P95 < 3 秒。
- 依赖 LLM 的复杂调用（面试准备、复盘）P95 < 10 秒。
- 前端页面首屏加载 < 2 秒（本地开发环境）。
- 错误必须返回统一格式 {code, msg, data}，禁止直接暴露堆栈。
